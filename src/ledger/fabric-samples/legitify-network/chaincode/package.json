{
    "name": "legitify-chaincode",
    "version": "1.0.0",
    "description": "Credential verification chaincode",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "engines": {
        "node": ">=14",
        "npm": ">=6"
    },
    "devDependencies": {
        "@babel/preset-env": "^7.24.0",
        "@babel/preset-typescript": "^7.24.0",
        "@types/jest": "^27.0.0",
        "@types/node": "^14.0.0",
        "@typescript-eslint/eslint-plugin": "^5.0.0",
        "@typescript-eslint/parser": "^5.0.0",
        "babel-jest": "^29.7.0",
        "eslint": "^8.0.0",
        "jest": "^29.7.0",
        "nodemon": "^2.0.0",
        "rimraf": "^3.0.2",
        "ts-jest": "^29.0.5",
        "typescript": "^4.9.5"
    },
    "scripts": {
        "build": "npx tsc",
        "prebuild": "npx tsc",
        "pretest": "npm run build",
        "test": "npx jest --config jest.config.js --forceExit --detectOpenHandles",
        "start": "fabric-chaincode-node start",
        "start:dev": "nodemon --watch ./dist --exec fabric-chaincode-node start",
        "lint": "eslint . --ext .ts",
        "clean": "rimraf dist node_modules package-lock.json",
        "prepare": "npx tsc"
    },
    "dependencies": {
        "fabric-contract-api": "^2.4.1",
        "fabric-network": "^2.2.20",
        "fabric-shim": "^2.4.1"
    }
}
